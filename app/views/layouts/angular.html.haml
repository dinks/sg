!!! 5
%html.no-js
  = render 'head'

  %body{ :class => "#{ controller.controller_name }", "ng-app" => "sgPicasaViewer"}
    = render 'chromeframe'
    = render 'angular_polyfill'
    .container
      = render 'header'
      = render 'flashes'

      = yield

      = render 'footer'

      -# Javascript at the bottom for fast page loading
      = render 'javascripts'
